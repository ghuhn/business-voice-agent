# ─────────────────────────────────────────────
# LiveKit — same project as agent
# ─────────────────────────────────────────────
VITE_LIVEKIT_URL=wss://YOUR_PROJECT.livekit.cloud

# Token generation endpoint (the agent server generates tokens)
# When running locally this is the agent's FastAPI endpoint
VITE_TOKEN_ENDPOINT=http://localhost:8080/token
